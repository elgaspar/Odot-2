<button class="btn text-secondary action-btn" data-toggle="modal" data-target="#categories-modal"
    data-name="{{ $category->name }}" data-color="{{ $category->color }}" data-id="{{ $category->id }}">
    <i class="fas fa-edit"></i>
</button>