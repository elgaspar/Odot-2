<button class="btn text-secondary action-btn" data-toggle="modal" data-target="#tasks-modal" data-name="{{ $task->name }}" data-category-id="{{ $task->category_id }}" data-parent-id="{{ $task->parent_id }}" data-id="{{ $task->id }}">
    <i class="fas fa-edit"></i>
</button>
