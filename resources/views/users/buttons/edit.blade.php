<button class="btn text-secondary action-btn" data-toggle="modal" data-target="#users-modal" data-name="{{ $user->name }}" data-email="{{ $user->email }}" data-is-admin="{{ $user->is_admin }}" data-id="{{ $user->id }}">
    <i class="fas fa-edit"></i>
</button>
